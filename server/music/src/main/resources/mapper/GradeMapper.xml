<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asuka.music.dao.GradeMapper">
    <resultMap id="BaseResultMap" type="com.asuka.music.domain.Grade" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="song_list_id" jdbcType="INTEGER" property="songListId"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="score" jdbcType="INTEGER" property="score"/>
    </resultMap>

    <insert id="insert" parameterType="com.asuka.music.domain.Grade">
        insert into grade
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null" >
                id,
            </if>
            <if test="songListId != null" >
                song_list_id,
            </if>
            <if test="userId != null" >
                user_id,
            </if>
            <if test="score != null" >
                score,
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id!=null">
                #{id},
            </if>
            <if test="songListId != null">
                #{songListId},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            <if test="score != null">
                #{score},
            </if>
        </trim>
    </insert>
    <select id="selectScoreSum" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        select COALESCE(sum(score),0) as sumscore from grade where song_list_id = #{songListId}
    </select>
    <select id="SelectUserSum" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        select COALESCE(count(id),0) as num from grade where song_list_id = #{songListId}
    </select>
    <select id="SelectSelfScore" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        select COALESCE(score,0) from grade where song_list_id = #{songListId} and user_id = #{userId}
    </select>

</mapper>