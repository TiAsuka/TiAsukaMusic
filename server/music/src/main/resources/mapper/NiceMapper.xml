<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asuka.music.dao.NiceMapper">
    <resultMap id="BaseResultMap" type="com.asuka.music.domain.Nice" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="comment_id" jdbcType="INTEGER" property="commentId"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
    </resultMap>

    <insert id="insert" parameterType="com.asuka.music.domain.Nice">
        insert into nice
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null" >
                id,
            </if>
            <if test="commentId != null" >
                comment_id,
            </if>
            <if test="userId != null" >
                user_id,
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id!=null">
                #{id},
            </if>
            <if test="commentId != null">
                #{commentId},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
        </trim>
    </insert>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from nice
        where id = #{id}
    </delete>

    <select id="SelectSelfNice" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        select id from nice where comment_id = #{commentId} and user_id = #{userId}
    </select>

</mapper>